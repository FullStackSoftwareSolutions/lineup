{
  "name": "lineup",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "type": "tsc --noEmit",
    "install:all": "npm install && npm --prefix ./db install && npm --prefix ./formatting install && npm --prefix ./native install && npm --prefix ./next install && npm --prefix ./whatsapp install"
  },
  "imports": {
    "@db/*": "./db/src/*",
    "@whatsapp/*": "./whatsapp/src/*",
    "@next/*": "./next/src/*",
    "@formatting/*": "./formatting/src/*"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  }
}
